<h2>Create New Course</h2>

<%= form_for :newCourse, url: courses_path, builder: FormBuilderWithDateTimeInput do |f| %>
  <%= f.error_messages %>

  <%= f.text_field :name,
  help_text: "The course ID to be used in URLs and DB keys. This field must be unique and URL-able. Typically includes the course number and semester. Examples: CSE115-f17, CSE116-f18, etc" %>

  <%= f.text_field :semester, size: 3,
  help_text: "The current semester. Examples: f12, s13, m13" %>

  <div class="form-group input-field">
    <%= text_field_tag :instructor_email, nil, class: "validate" %>	
    <label class="control-label" for="instructor_email">Instructor Email</label>
		<p class="help-block">Email of an Instructor, they will set up the course from here!</p>
  </div>

  <%= f.submit "Create" , {:class=>"btn primary"} %>
<% end %>
